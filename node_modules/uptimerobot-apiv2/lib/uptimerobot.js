"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _client = require("./client");

var _client2 = _interopRequireDefault(_client);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const METHODS = ["getAccountDetails", "getMonitors", "newMonitor", "editMonitor", "deleteMonitor", "resetMonitor", "getAlertContacts", "newAlertContact", "editAlertContact", "deleteMonitor", "getMWindows", "newMWindow", "editMWindow", "deleteMWindow", "getPSPs", "newPSP", "editPSP", "deletePSP"];

class UptimeRobot {
  constructor(key) {
    if (!key || typeof key !== "string") {
      throw new Error("UptimeRobot API key must be provided.");
    }
    this.__client = new _client2.default(key);
  }
}

for (let method of METHODS) {
  UptimeRobot.prototype[method] = function (options) {
    return this.__client.request(method, options);
  };
}

exports.default = UptimeRobot;